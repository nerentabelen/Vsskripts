# learn.javascript.ru   --!
# code-basics.com   --!

Модуль 2 - Основа JavaScript.
Программы написанные на JS - называются "скриптами".
Не стоит путать Java и JavaScript - это два разных языка программирования.
JS предаёт функционал(динамику) на Web-страницах(Web-игра). С помощью JS можно создавать динамические HTML и CSS элементы.

alert("Hello, World!") 
Данная функция выводит какой либо результат в виде всплывающего окна.

console.log("Hello, World!") - выводит какой-либо результат в консоль страницы.

    !1 тема: Переменные
Переменная - ячейка памяти для хранения данных.

3 основных оператора объявления переменных: "let", "var", "const".
let - локальная переменная(любой тип данных)
var - глобальная переменная(любой тип данных)
const - локальная константа(неизменняемая; непереопределяемая переменная)
(a = 3, b = 5, c = 4;
alert(a+b))

    !2 тема: Типы данных.
Существует несколько типов данных в JS:

a = b  ---> Number, int
b = "Hello!" ---> String(строка)
c = true(or false) ---> Boolean (булевы)
d = [3, "test", 5.5] ---> Массив данных(array) (в массивах, отсчёт начинается с нуля)
e = {name: "dawg", age: 28} ---> Объект 

f = a+a = 10
g = b+b = "Hello! Hello!" - процесс называется
h = a+b = 5Hello!
i = a+c = 5 (in green(or blue))

a = 5, b = "hello.", c = 3.14, d = false
console.log(c)

null - вывод неизвестной переменной
undefined - вывод неизвестной переменной в консоль

    !3 тема: Встроенные функции взаимодействия с пользователем.
alert()
prompt() - Вызывает всплывающее окно для ввода данных с клавиатуры.
confirm() - Вызывает всплывающее окно для подтверждения действий пользователем.

Example of module work:
userName = prompt()
alert("Hello, " + userName)

    !4 тема: Преобразование типов данных 
JavaScript автоматически выполняет преобразования типов в различных контекстах, но также он позволяет явно выполнить такие преобразования:
a = "123", (string)
b = 5,
newA = Number(a) (превращает a - в число)
newB = String(b) (превращает b - в строку)
console.log(newA+b)
console.log(a+newB)

c = 3,14,
newC = parseInt(c); (превращает число с десятыми в целое)
console.log(newC)

Number - пытается преобразовать переменную в числовой тип данных.
String - пытается преобразовать переменную в строчный тип данных.
parseInt - пытается преобразовать числовой тип данных и убирает числа после запятой.
parsefloat - пытается преобразовать числовой тип данных и не убирает числа после запятой.


Тема5 Математические операции

let x = 10;
let y = 5;
let z = "first";
let x = "second";
console.log(x+y); - сложение
console.log(x-y); - вычитание 
console.log(x*y); - умножение
console.log(x/y); - деление
console.log(x**y); - возведение в степень
console.log(x%y); - вывод оста


задача:
напишите программу кеоторая запрашивает у пользователя его возраст с клавиатуры и выводит сообщение с указание сколько лет пользователю через 10 лет
let x = prompt("введите возраст");
let b = Number(x) + 10;
let z = "через 10 лет вам будет ";
console.log(z + b);

тема 6 операторы сравнения

1 < условие верно если первый оперант меньше второго (true)
2 > условие верное если первый оперант больше второго
3 <= условие верное если первый оперант меньше или равен второму
4 >= условие верное если первый оперант больше или равен второму
5 == условие верное если операнты равны
6 === условие верное если оба операнта равны и имеют одинаковый тип данных 

тема 7 условные операторы (if,else)


условные операторы позволяют выполнять различные действия в зависимости от выполнения определенных условий.
основные операторы в js это if(), else() if{}, else{}.
инструкция if() вычесляет выражение в круулых скобках и преобразует результат к логическому типу 
let x = + prompt("введите число");
if(x>5) {
console.log("икс больше 5");
}
else if(x===5){
console.log("икс равен 5")
}
else{
console.log("икс меньше 5")
}

можно просто писать if все будет также работать

применение тернанного оператора
let x = 3;
let message = (x>5)? "x больше 5": "x меньше или равен 5";
console.log(message); 
 

 тема 8 Логические операторы 



используются для комбинирования или изменения логических значений 
позволяют создавать более сложные условия для проверки
&& - логическое "и" или *
|| - логическое "или" или +
 !=  - не этот знак

 задачки на дом:
1 напишите программу которая запрашивает у пользователя число и проверяет принадлежит ли число диапозону от 10(включительно) до 100 рез в консоль
2 напишите программу которая запрашивает у пользователя число и выводит сообщение о том четное число или не четное 
3 напишите программу которая проверяет возраст пользователя введенный с клавиатуры  если возраст меньше 18 лет то в консоль пишет что доступ запрещен else доступ разрешен c применением тернарного оператора 



напишите задачу которая запрашивает три запроса или три сторорны треугольника 
и в результат выводит какой треугольник
чтобы решить 

напиши программу кльлрая
есть система кллрдинат ху, в какой координатной четверти лежит точка, нужна четверть которая лежит точка


let x = parseFloat(prompt("введите x"));
let y = parseFloat(prompt("введите y"));
if (x > 0 && y> 0){
    console.log("I четверть");
}
else if (x <0 && y>0){
    console.log("II четверть");
}
else if (x < 0 && y<0){
    console.log("III четверть");
}
else if (x > 0 && y<0){
    console.log("IV четверть");
}
else if (x===0 && y===0){
    console.log("начало координат");
}


while // for
зачастую встает задача сделать одно и тоже действие много раз 
 например, вывести товар из списка одним за другим или просто перебрать все числа от 1 до 10 и для каждого выполшнить одинаковый код например сделать счетчик
 у нас етсь кнопка и каждый последую клик будет выдавать 1 потом 2 потом 3 
 цикл while: первое цикл while проверяет блок кода, пока указанно условие истиннор 
 сначало проверяется увловие перед выполнением блока кодаесли условие истинное блок кода выполняется. после выполнение блока кода условие сново проверяется.
  процесс повторяется до тех пор пока условие не станет ложным


  let i=0
while (i < 5){
    alert (i)
    i=i+1 // i++
}


есть еще конструкция do while
пример использования: 

let i = 0
do {
    alert(i)
    i=i+1 }
    while (a < 5)


оператор do позволяет гарантированно выполнить одну операцию после нее начнется цикл while 

цикл for позволяет более компатный цикл 
for состоит из трех частей 
For (let i=0; i<5; i++)
1 инициализация
2 условие
3 выражение

for(let i=0; i<5; i++)
{alert (i) }


1 задача 

let login = prompt("Введите ваш логин");
let password = prompt("Введите ваш пароль");
if (login === "admin" && password === "password"){
    console.log("Доступ разрешен");
}else{
    console.log("Отказанно в доступе");
}

2 задача 

let number = + prompt("Введите число");
let sum = 0;
for(let i = 1; i <= number; i++){
    sum = sum + i;
    console.log(sum)
}

3 задача


let n = + prompt("Введите число")

if (n % 2 === 0){ //если n = четное

  let sum = 1
  for (let i = 1; i< n; i++) //счетчик

 {
     sum = sum + i;
     console.log(sum);
 }
}
else {
    let multiplication = 1;
    for (let i = 1; i < n; i++)
    {
        multiplication = multiplication * i;
        console.log(multiplication)
    }
}






                 при рывания цикла (break) и продолжение continue


                 let sum = 0

while (true) {
    let value = + prompt("Введите число")

    if (!value) break;
    sum = sum + value 
}
alert (sum);


обычно цикл звершается при вычисление условия внутри значения false.
но мы можем выйти из цикла в любой момент с помощью специальной оператора break
пример программа просчитывает сумму вводимых чисел до тех пор, пока пользовател ь вводит их с клавиатуры 

оператор break полностью прекращает выполнени цикла и передает выполнение на блок кода за его телом (например на alert)
 следущий опертор continue
 при выполнени оператора continue цикл не заершвается а переходит к следущей интерации (если услогвие все еще = true).
  с помощью цикла for и оператора continue чтобы вывести только нечетные значения от 0 до 10
если i % 2 =i это при случии если значение четное мы его пропускаем и у нас должны выполняться alert 
for (let i = 0; i < 10; i++) {
if (i % 2 == o) break;
alert(i)
}

конструкция switch и case default
используется для выполнения различных действий в зависимости значения выражения 
она представляет собой альтернативу цепочки if else и обычно используется когда нужно сравнивать одно значени или со множествами вариантов 
позволяет проверить конретику 
имбуля
с клавиатуры вводится число, если число = 3 то выводим резултат "мало"
если число = 4 то выводи результат"в точку"
если число = 5 то "много"
если ! =3,4,5 

ПРИМЕР 
let a = +prompt();

switch (a) {
    case 3:
        alert("Мало");
        break;
    case 4:
        alert("В точку");
        break;
    case 5:
        alert("Много");
        break;
        default:
            alert("МММ");
            break;
}

пример экономия памяти 
let a = +prompt();

switch (a) {
    case 3:
    case 5:
        alert("Рядом");
        break;
    case 4:
        alert("В точку");
        break;
    
    default:
            alert("МММ");
            break;
}


задание на дом на switch case 
С клавиатуры ВВОДИТСЯ число от 0 до 3 включительно; с помощью конструкции switch и case выведите результат. если 
введеное число = 0 то вы ввели число 0
елси 1 то программа выведит число 1
если число 2 или 3 программа выводит число 2 или 3
при вводе других чисел программа выдаст результат число в НЕ ДИАПОЗОНЕ ОТ 0 до 3


let a =+prompt();

switch(a){
    case 3:
    case 2:
        alert("ввели 2 или 3");
        break;
    case 0:
        alert("0");
        break;
    case 1:
            alert("1");
            break;
    default:
        alert("вне диапозона");
        break
    }




            функции по java skript
переменные обьявленные внутри 
переменные обьявленные до функции видны за пределами функции
параметры функции это данные которые могут быть переданны в функцию в виде каких либо значений или перемен

вызов с аргумаентом:

function func(a, b){
    console.log(a+b)
}
func(3, 5) 

другой пример:

function func(a, b){
    return a + b
}
let sum = func(3, 5)
console.log(sum)

еще пример:

function func(a, b){
    return a + b
}
console.log(func(3,5))



2. функция function expression
когда функция создается как выражение 

пример:

let sum = function(a, b) {
    return a + b
}
console.log(sum(3, 4))



3. стрелочная функция 
происхиодит конфертается параметров а стрелка это то что происходит в теле функции

пример:

let sum = (a, b) => a + b

console.log(sum(5,3))


-------
let func = (name) => {
    let message = "Привет, " + name
    console.log(message)
}

func('Ярослав')



4. метод 
программа считает сумму значений аргументов по количеству аргумаентов:

function  sumAll() {
    let sum = 0
    for (let i = 0; i < arguments.length; i++){
        sum = sum + arguments[i]
    }
    return sum
}

console.log(sumAll( 1, 2, 3, 4))

напишите функцию которая принимает число с клавиатуры и результат выдает квадрат этого числа:

function func() {
let a = +prompt ("введите число");
a = a**2;
alert(a);
}
 
func()


с применением синтаксичных стрелочных


let fuv = (+prompt("введите число"));
fuv = fuv**2;
alert(fuv);


let fkg = (num) => {
        num = num ** 2
        console.log(num)
}
 
fkg(prompt())
 

 напишите функцию которая принимает два числа с клавиатуры и возращает большее из них \

 самый обычный: 
 function lk() {
    let a = +prompt("Введите первое число");
   let b = +prompt("Введите второе число"); 
  if (a > b) {
    return a;
} else {
    return b;
}
}

let result = lk();
alert("Большее число = " + result);


стрелочный: (самый лучший)
let numCheck = (num1, num2) => {
        if(num1 > num2)
                console.log(num1)
        else if(num1 < num2)
                console.log(num2)
        else{console.log("they are equal")}
}
 
numCheck(prompt(), prompt())

домашка задачка йоу 

С клавиатуры вводится число от 0 до 3 включительно с помощью конструкции switch и case выведите результат,
если введное число 0 = то 0
если введеное число 1 = то 1
если введенное число 2 или 3 то программа покажет вы ввели 2 и 3
при вводе других чисел программа выдаст результат число вне диапазона от 0 до 3

РЕАЛИЗОВАТЬ С ПОМОЩЬЮ ФУНКЦИИ. При этом, одна функция должна быть БЕЗ ПАРАМЕТРОВ/АРГУМЕНТОВ, а вторая функция - с параметрами/аргументами.
 
+ Решать задания code-basics.com (создать уч.запись). 

с аргументом

function lx(n) {
    switch (n) {
        case 0:
            alert("0");
            break;
        case 1:
            alert("1");
            break;
        case 2:
        case 3:
            alert("вы ввели 2 и 3");
            break;
        default:
            alert("число вне диапазона от 0 до 3");
    }
}
lx(parseInt(prompt("Введите число от 0 до 3:")));

без аргумента

function lx() {
    let number = parseInt(prompt("введи число от 0 до 3"));
    
    switch (number) {
        case 0:
            alert("0");
            break;
        case 1:
            alert("1");
            break;
        case 2:
        case 3:
            alert("Вы ввели число от 2 до 3");
            break;
        default:
            alert("вне диапозона от 0 до 3");
    }
}

lx();


1 задача пользователь вводит сумму покупки с клавиатуры, если сумма покупки от 1000 до 2000 то скидка будет 10 процентов, 
если сумма покупки от 2000 до 5000 то скидка 15 процетов если покупка составляет более 5000 то скидка 30 процентов.
Решить с помощью конструкции if else, если сумма меньше 1000 то без скидки (0%)
//задача 1
let sum = + prompt("Введите сумму")
if (sum < 1000) {
    alert ("Скидка 0%")
} 
else if (sum >= 1000 && sum <= 2000) {
    alert ("Скидка 10%")
} 
else if (sum >= 2000 && sum <= 5000) {
    alert ("Скидка 15%")
} 
else {
    alert ("Скидка 30%")
}


2 задача пользователь вводит от 1 до 7 с клавиатуры используюя свич вывести название дня недели.
// задача 2
let number = parseInt(prompt("введи число от 1 до 7"));
    
switch (number) {
    case 1:
        alert("Понедельник");
        break;
    case 2:
        alert("Вторник");
        break;
    case 3:
        alert("Среда");
        break;
    case 4:
        alert("Четверг");
        break;
    case 3:
        alert("Пятница");
        break;
    case 3:
        alert("Суббота");
        break;
    case 3:
        alert("Воскресенье");
        break;
    default:
        alert("Вы ввели число вне диапозона от 1 до 7");
}


3 задача вывести все простые числа от 2 до 100, прсотое число это которое делится на 1 и на само себя через if else/

4 задача программа загадывает случайное число от 1 до 100, пользователь пытается написать овтет если число мало меньше загаданного
то будет выводить мало если угадал то в точку если больше загаданного то будет писать много.
нужно сделать через while, break, Math.random(a) 

5 задача калькулятор оценок, пользователь вводит с клавиатуры баллы по 3 предметам. 
Программа должна выводить результутом среднюю оценку баллы идут от 10 до 100 и вывести оценку и есть градация
89-100 это 5
75-90 это 4
60-74 это 3
меньше 60 это 2
//задача 5
let num1 = + prompt ("Введите балл за первый предмет")
let num2 = + prompt ("Введите балл за второй предмет")
let num3 = + prompt ("Введите балл за третий предмет") 
let result
result = num1 + num2 + num3
let sum = result / 3
 if (result >= 90 && result <= 100){
    alert ("5")
}
else if (result >= 75 && result <= 89){
    alert ("4")
}
else if (result >= 60 && result <= 75){
    alert ("3")
}
else{
    alert("2")
}

6 задача с клавиатуры вводится число программа должна проверить есть ли буква К в этом слове
использовать функцию include(a) 
решить с помощью тернанного оператора 

7 задача с клавиатуры вводится слово вывести результат в консоль число длинну этого слова число типо сколько букв
//задача 7
let string = prompt ("Введите слово")
let count = string.length 
alert (count) 
